
name: x_bot
description: X Bot - Binance al-sat mobil uygulaması
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^0.13.6
  web_socket_channel: ^2.4.0
  provider: ^6.1.2
  intl: ^0.19.0
  fl_chart: ^0.66.0
  flutter_secure_storage: ^9.0.0
  flutter_dotenv: ^5.1.0
  syncfusion_flutter_charts: ^25.1.35
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/
